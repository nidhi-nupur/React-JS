import React from 'react'

export default function Header() {
  return (
    <div>
        <h1>This is Header Section</h1>
        <h2>Each and every html tag must have their parent tag otherwise it'll show an error. Or the other way to use two or more tags together without any parent tag, to use those components enclosed with div tag.</h2>
    </div>
    
  )
}
